const buzz = require('./buzz')
const httpsServe = require('./https-serve')
const proSearch = require('./pro-search')
const colorConvert = require('./color-convert')
const codeED = require('./code-d-e')

module.exports = {
  buzz,
  httpsServe,
  proSearch,
  colorConvert,
  codeED,
}